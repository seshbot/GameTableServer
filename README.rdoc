== README

This is intended to be a starting point for a Rails 4 + Ember.js 1.3 application that has simple authentication built in. 

Because it's a standard rails app, just run the usual `rake db:create; rake db:migrate; rails s` to see it in action.

